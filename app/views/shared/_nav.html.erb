<nav class="mb-6 border-b border-slate-200 pb-4 flex items-center gap-6 text-sm">
  <%= link_to "DMs", root_path,
        class: "font-medium #{controller_name == 'dashboard' ? 'text-slate-900' : 'text-slate-500 hover:text-slate-800'}" %>
  <%= link_to "Orders", orders_path,
        class: "font-medium #{controller_name == 'orders' ? 'text-slate-900' : 'text-slate-500 hover:text-slate-800'}" %>
  <%= link_to "Products", products_path,
        class: "font-medium #{controller_name == 'products' ? 'text-slate-900' : 'text-slate-500 hover:text-slate-800'}" %>
  <%= link_to "Search test", search_test_path,
        class: "font-medium #{controller_name == 'search_test' ? 'text-slate-900' : 'text-slate-500 hover:text-slate-800'}" %>
</nav>
